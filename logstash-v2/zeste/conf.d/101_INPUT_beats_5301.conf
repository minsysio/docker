input {
	beats {
		port => "5301"
    		type => "klima_beats_type"
    		ssl => true
    		ssl_certificate => "/etc/pki/tls/certs/logstash-filebeat.crt"
    		ssl_key => "/etc/pki/tls/private/logstash-filebeat.key"
	}
}